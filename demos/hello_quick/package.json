{
  "name": "posit-92_assemblyscript",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "assemblyscript": "^0.28.9"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "compile": "npm run asbuild:debug",

    "asbuild:debug": "asc index.ts --target debug",
    "asbuild:release": "asc index.ts --target release",
    "asbuild": "npm run asbuild:debug && npm run asbuild:release",
    
    "test": "node tests",
    "start": "npx serve ."
  }
}